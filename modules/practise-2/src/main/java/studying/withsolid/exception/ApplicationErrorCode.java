package studying.withsolid.exception;

/** Codes of the application level errors. */
public enum ApplicationErrorCode {
    /** Input data did not pass validation. */
    VALIDATION_ERROR,
    /** Report could not be written to the file system. */
    FILE_WRITE_ERROR
}
